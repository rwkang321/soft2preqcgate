spring:
  application:
    name: soft2preqcgate
  profiles:
    # 로컬 기본값만 dev. 운영 배포는 SPRING_PROFILES_ACTIVE를 명시한다.
    default: dev

  # datasource, Redis, Flyway, MyBatis, auth, CORS의
  # 환경별 실제 값은 application-dev.yaml/application-prod.yaml에서 관리한다.
