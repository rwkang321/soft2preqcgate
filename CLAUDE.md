# Agent Guidelines &amp; Boundaries



## I. 일반 지침

## 1. 엄격한 금지 사항 (Strictly Forbidden)

- **애플리케이션 소스 코드 직접 수정/생성 금지**:
  - `src/` 하위의 모든 소스 코드 (Java, Kotlin, XML, SQL, Vue, React, Nexacro 스크립트 등)
  - 빌드 설정 파일 `build.gradle`, `pom.xml`, `settings.gradle` 등)
  - 소스 코드는 오직 분석만 수행하고, 수정 제안은 답변 텍스트(Markdown 코드 블록)로만 제공할 것.

## 2. 허용 및 자율 수행 작업 (Allowed Operations)

- **문서 및 리포트 작성**: 분석 보고서, [마크다운`.md`](http://마크다운.md)), UML, 설계 문서의 생성 및 수정 허용.
- **Orca 오케스트레이션 및 작업 관리**: 
  - Orca 워커(Worker) 생성/관리, 하위 에이전트 실행 및 조율.
  - Git 작업 (Worktree 생성, 브랜치 관리, 커밋 등 오케스트레이션에 필요한 Git 명령).
- **조회 및 분석**: 전체 프로젝트 파일의 읽기(Read), 검색, 구조 분석.

## 3. 보고서 파일 작성 지침

- **보고서는 \docs\reports\ 폴더에 \backend\와 \frontend\로 구분하여 저장한다.**
- **보고서는 사용자가 정확하게 이해 할 수 있도록, 단축 단어 사용 또는 함축 용어 사용을 절대 피한다.**
- **애매모호한 내용에 대해 장황한 설명은 하지 말고, 프로젝트 진행에 꼭 필요한 내용에 대해서만 집중해서 설명한다.**
- 보고서 코드마다 반드시 전체 교체, 기존 코드 바로 아래에 추가, 특정 분기 안에 삽입, 삭제 중 어떤 작

    업인지 명시하고, 수정 전후의 충분한 주변 문맥까지 제시
- **보고서 작성 시, 기존 소스 수정은 아래와 같이, 수정해야 할 현재 내용과 수정해야 할 내용으로 표기한다.** 
- 보고서 저장 위치와 이름은 backend 관련해서는 \docs\reports\backend\ 폴더에, frontend 관련해서는 \docs\reports\nexacro\폴더를 사용하고, 파일명은 "프로젝트명(soft2cost2)_오늘날짜(20260830)_현재시각에서 시간,분(1521)".md 파일로 한다. 즉 "soft2cost_20260830_1521.md" 이런식이다.

## 4. 작성된 보고서 파일에 대한 피드백 질의 지침

- **3번 항에서 작성된 보고서에 대해, 추가 질의에 대한 답변은 가능한 실제 예시를 들어, 간결하게 단답형으로 제공한다.**
- **피드백 질의 응답에 대해서는 마스터에게 보고 하지 않는다.**



## II. 코딩 지침

## 1. Service

- **모든 Service  는 먼저 Interface 파일을 만들고, class 파일로 구현하는 패턴으로 코딩한다.**
- 

